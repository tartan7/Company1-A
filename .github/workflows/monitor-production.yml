name: Monitor Production

on:
  workflow_dispatch:
  schedule:
    - cron: "*/30 * * * *"

jobs:
  monitor:
    name: Uptime and TLS Checks
    runs-on: ubuntu-latest
    environment: production

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Validate monitor script
        run: test -x scripts/monitor-production.sh

      - name: Run production checks
        env:
          PROD_BASE_URL: ${{ vars.PROD_BASE_URL }}
          ALERT_WEBHOOK_URL: ${{ secrets.PROD_ALERT_WEBHOOK_URL }}
          TLS_MIN_VALID_DAYS: ${{ vars.TLS_MIN_VALID_DAYS }}
        run: |
          set -euo pipefail
          if [ -z "${PROD_BASE_URL:-}" ]; then
            echo "::warning::PROD_BASE_URL is not set in repository/environment variables; skipping monitor checks."
            exit 0
          fi
          bash scripts/monitor-production.sh "$PROD_BASE_URL"
